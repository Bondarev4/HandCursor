# 📊 Моделирование проекта HandCursor

## 🧠 Ментальная карта проекта

```mermaid
mindmap
  root((HandCursor Pro))
    Архитектура
      Модули
        Ввод данных
          Захват видео
          Предобработка
          Буферизация
        Компьютерное зрение
          Детекция руки
          Трекинг движения
          Стабилизация
        Машинное обучение
          Классификация жестов
          Обучение модели
          Извлечение признаков
        Управление
          Обработка координат
          Генерация событий
          Интеграция с ОС
      Технологии
        Python 3.9+
        OpenCV 4.5+
        MediaPipe 0.8+
        TensorFlow 2.8+
        PyAutoGUI
        PyQt5
    
    Функциональность
      Основные функции
        Управление курсором
          Перемещение
          Точное позиционирование
          Сглаживание движений
        Эмуляция мыши
          Левый клик
          Правый клик
          Двойной клик
          Drag & Drop
        Скроллинг
          Вертикальный
          Горизонтальный
      Дополнительные
        Калибровка
          Автоматическая
          Ручная
          Сохранение профилей
        Настройки
          Чувствительность
          Скорость курсора
          Пороги жестов
    
    Пользователи
      Основные
        Люди с ограниченными возможностями
        Специалисты (врачи, преподаватели)
        Разработчики
      Дополнительные
        Геймеры
        Пользователи в гигиеничных средах
        Любители технологий
    
    Разработка
      Этапы
        Исследование
          Анализ существующих решений
          Выбор технологий
          Прототипирование
        Реализация
          Разработка ядра
          Создание интерфейса
          Тестирование
        Оптимизация
          Улучшение производительности
          Повышение точности
          Расширение функциональности
      Методологии
        Итеративная разработка
        Модульное тестирование
        Непрерывная интеграция
```

## 🔄 Декомпозиция (IDEF0)

```mermaid
flowchart TD
    subgraph A0 [A0: Управление компьютером жестами руки]
        A1[Захват и обработка<br/>видео]
        A2[Детекция и трекинг<br/>руки]
        A3[Классификация<br/>жестов]
        A4[Генерация и выполнение<br/>команд]
        
        A1 --> A2 --> A3 --> A4
    end
    
    I1[Вход: Видеопоток] --> A1
    C1[Управление: Настройки камеры] --> A1
    M1[Механизмы: Камера, OpenCV] --> A1
    
    I2[Вход: Обработанное видео] --> A2
    C2[Управление: Параметры детекции] --> A2
    M2[Механизмы: MediaPipe] --> A2
    
    I3[Вход: Landmarks руки] --> A3
    C3[Управление: Модель жестов] --> A3
    M3[Механизмы: TensorFlow] --> A3
    
    I4[Вход: Распознанные жесты] --> A4
    C4[Управление: Маппинг жестов] --> A4
    M4[Механизмы: PyAutoGUI] --> A4
    
    A1 --> O1[Выход: Обработанное видео]
    A2 --> O2[Выход: Landmarks руки]
    A3 --> O3[Выход: Распознанные жесты]
    A4 --> O4[Выход: Команды ОС]
```

## 📈 DFD Диаграмма контекста 0

```mermaid
flowchart TD
    Пользователь[Пользователь] -->|Жесты руки| Система[Система HandCursor]
    Система -->|Управление курсором| Операционная_система[Операционная система]
    Операционная_система -->|Визуальная обратная связь| Пользователь
    Внешняя_память[Хранилище данных] -->|Профили калибровки| Система
    Система -->|Статистика использования| Внешняя_память
```

## DFD Диаграмма контекста 1

```mermaid
flowchart TD
    subgraph Система_HandCursor [Система HandCursor]
        Процесс1[Модуль захвата видео]
        Процесс2[Модуль обработки жестов]
        Процесс3[Модуль управления курсором]
        Процесс4[Модуль пользовательского интерфейса]
        
        Хранилище1[Хранилище: Настройки]
        Хранилище2[Хранилище: Профили жестов]
        Хранилище3[Хранилище: История жестов]
    end
    
    Камера[Внешняя сущность: Камера] -->|Видеопоток| Процесс1
    Процесс1 -->|Обработанные кадры| Процесс2
    Процесс2 -->|Распознанные жесты| Процесс3
    Процесс3 -->|Команды мыши| ОС[Внешняя сущность: ОС]
    
    Пользователь[Внешняя сущность: Пользователь] -->|Настройки интерфейса| Процесс4
    Процесс4 -->|Параметры калибровки| Хранилище1
    Хранилище1 -->|Настройки системы| Процесс2
    
    Процесс2 -->|Жесты для обучения| Хранилище2
    Хранилище2 -->|Шаблоны жестов| Процесс2
    
    Процесс2 -->|Запись жестов| Хранилище3
    Хранилище3 -->|Статистика использования| Процесс4
```

## Модель в нотации BPMN

```mermaid
flowchart TD
    subgraph Основной_процесс [Процесс управления жестами]
        Начало([Начало процесса]) --> Калибровка{Система откалибрована?}
        
        Калибровка -->|Нет| Процесс_калибровки[Процесс калибровки]
        Процесс_калибровки --> Сохранение_профиля[Сохранить профиль калибровки]
        Сохранение_профиля --> Загрузка_профиля
        
        Калибровка -->|Да| Загрузка_профиля[Загрузить профиль пользователя]
        
        Загрузка_профиля --> Основной_цикл
        
        subgraph Основной_цикл [Основной цикл обработки]
            Захват_кадра[Захват кадра с камеры]
            Предобработка[Предобработка изображения]
            Детекция_руки[Детекция руки и landmarks]
            Классификация_жеста[Классификация жеста]
            Генерация_команды[Генерация команды]
            Выполнение_команды[Выполнение команды в ОС]
            Обратная_связь[Визуальная обратная связь]
            
            Захват_кадра --> Предобработка --> Детекция_руки --> Классификация_жеста --> Генерация_команды --> Выполнение_команды --> Обратная_связь
        end
        
        Обратная_связь --> Проверка_продолжения{Продолжать работу?}
        
        Проверка_продолжения -->|Да| Захват_кадра
        Проверка_продолжения -->|Нет| Конец([Конец процесса])
    end
    
    subgraph Обработка_ошибок [Обработка ошибок]
        Ошибка_детекции[Ошибка детекции руки]
        Ошибка_классификации[Ошибка классификации жеста]
        Ошибка_выполнения[Ошибка выполнения команды]
        
        Ошибка_детекции --> Перекалибровка[Предложить перекалибровку]
        Ошибка_классификации --> Использование_резервного[Использовать резервный жест]
        Ошибка_выполнения --> Восстановление[Восстановление состояния]
    end
    
    Основной_цикл -.->|При ошибке| Обработка_ошибок
    Обработка_ошибок -.->|После обработки| Основной_цикл
```

## Модели в нотации UML - структурная

```mermaid
classDiagram
    class HandCursorSystem {
        -VideoCapture videoCapture
        -HandDetector handDetector
        -GestureClassifier gestureClassifier
        -CursorController cursorController
        -CalibrationManager calibrationManager
        +start()
        +stop()
        +pause()
        +calibrate()
    }
    
    class VideoCapture {
        -int cameraId
        -tuple resolution
        -int fps
        +openCamera()
        +readFrame()
        +preprocessFrame()
        +closeCamera()
    }
    
    class HandDetector {
        -float detectionConfidence
        -float trackingConfidence
        -int maxHands
        +detectHands(image)
        +getLandmarks()
        +drawLandmarks()
    }
    
    class GestureClassifier {
        -Model gestureModel
        -dict gestures
        -float threshold
        +loadModel(path)
        +classify(landmarks)
        +getConfidence()
        +addGesture()
    }
    
    class CursorController {
        -tuple screenSize
        -float sensitivity
        -int smoothing
        +moveCursor(x, y)
        +click(button)
        +doubleClick()
        +drag(start, end)
        +scroll(direction)
    }
    
    class CalibrationManager {
        -Profile currentProfile
        -list~Profile~ profiles
        +startCalibration()
        +saveProfile()
        +loadProfile()
        +getCurrentSettings()
    }
    
    class Gesture {
        +string name
        +string code
        +string action
        +float threshold
        +isRecognized(landmarks)
    }
    
    class Profile {
        +string name
        +float sensitivity
        +int smoothing
        +dict gestureMappings
        +datetime created
        +bool isDefault
    }
    
    HandCursorSystem --> VideoCapture
    HandCursorSystem --> HandDetector
    HandCursorSystem --> GestureClassifier
    HandCursorSystem --> CursorController
    HandCursorSystem --> CalibrationManager
    GestureClassifier --> Gesture
    CalibrationManager --> Profile
```

## Модели в нотации UML - поведенческая 1

```mermaid
sequenceDiagram
    participant User as Пользователь
    participant System as Система HandCursor
    participant VC as VideoCapture
    participant HD as HandDetector
    participant GC as GestureClassifier
    participant CC as CursorController
    participant OS as Операционная система
    
    User->>System: Запускает систему
    System->>VC: Инициализация камеры
    VC-->>System: Камера готова
    System->>HD: Загрузка модели детекции
    System->>GC: Загрузка модели жестов
    System-->>User: Система запущена
    
    loop Основной цикл обработки
        VC->>VC: Захват кадра (30 FPS)
        VC->>HD: Передача кадра
        HD->>HD: Детекция руки
        HD->>HD: Извлечение landmarks
        HD->>GC: Передача landmarks
        
        alt Рука обнаружена
            GC->>GC: Классификация жеста
            GC->>GC: Проверка уверенности
            GC->>CC: Распознанный жест
            
            CC->>CC: Преобразование координат
            CC->>CC: Фильтрация движений
            CC->>OS: Генерация команды
            OS-->>User: Выполнение действия
        else Рука не обнаружена
            GC->>System: Сигнал отсутствия руки
            System->>User: Визуальная подсказка
        end
        
        System->>User: Визуальная обратная связь
    end
    
    User->>System: Останавливает систему
    System->>VC: Освобождение камеры
    System->>HD: Освобождение ресурсов
    System->>GC: Освобождение моделей
    System-->>User: Система остановлена
```

## Модели в нотации UML - поведенческая 2

```mermaid
stateDiagram-v2
    [*] --> Неактивно
    
    Неактивно --> Калибровка : Пользователь запускает
    Калибровка --> Активно : Калибровка завершена
    Активно --> Пауза : Пользователь ставит на паузу
    Пауза --> Активно : Пользователь возобновляет
    
    state Активно {
        [*] --> Ожидание_жеста
        Ожидание_жеста --> Детекция_руки : Кадр получен
        
        state Детекция_руки {
            [*] --> Поиск_руки
            Поиск_руки --> Рука_найдена : Рука обнаружена
            Поиск_руки --> Рука_не_найдена : Рука не найдена
            Рука_найдена --> Извлечение_landmarks
            Извлечение_landmarks --> Классификация
            Рука_не_найдена --> Ожидание_жеста
        }
        
        Классификация --> Жест_распознан : Уверенность > порога
        Классификация --> Жест_не_распознан : Уверенность < порога
        
        Жест_распознан --> Выполнение_команды
        Выполнение_команды --> Обратная_связь
        Жест_не_распознан --> Ожидание_жеста
        Обратная_связь --> Ожидание_жеста
    }
    
    Активно --> Калибровка : Пользователь перекалибровывает
    Активно --> Неактивно : Пользователь останавливает
    Пауза --> Неактивно : Пользователь останавливает
```
      - pinch: "left_click"
      - victory: "right_click"
      - fist: "drag"
      - thumbs_up: "scroll_up"
